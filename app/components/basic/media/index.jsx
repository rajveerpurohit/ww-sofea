import React from 'react';
import { EXTERNAL_IMAGE_URL, INTERNAL_IMAGE_URL, USE_CDN } from '../../../../config/app'; 


const Media = (props) => {
    console.log('Media - ', EXTERNAL_IMAGE_URL);
    return (
      <img />
    );
};
export default Media;
